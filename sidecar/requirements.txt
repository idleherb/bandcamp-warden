fastapi==0.118.0
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
docker==7.1.0
httpx==0.28.1
apscheduler==3.10.4
# bandcampsync handles all the Fan API auth and pagination — TLS
# impersonation, session cookie handling, paged collection load,
# de-duplication. We use the same library the bandcampsync container
# uses, so identical behaviour. curl_cffi comes in transitively.
bandcampsync==0.7.0
# Playwright (Plan D) is opt-in: only used by the test endpoint.
# Not installed by default to keep image small. To enable:
#   1. uncomment the line below
#   2. add Chromium/Firefox apt deps + `python -m playwright install` to Dockerfile
# playwright==1.50.0
