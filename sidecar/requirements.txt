fastapi==0.118.0
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
docker==7.1.0
httpx==0.28.1
apscheduler==3.10.4
# bandcampsync handles all the Fan API auth and pagination — TLS
# impersonation, session cookie handling, paged collection load,
# de-duplication. We use the same library the bandcampsync container
# uses, so identical behaviour. curl_cffi comes in transitively.
bandcampsync==0.7.0
# Playwright for browser-driven downloads (Plan D). Pulled because
# Bandcamp throttles non-browser HTTP clients to 0 B/s session-wide.
# Chromium + Firefox installed in Dockerfile via `playwright install`.
playwright==1.50.0
