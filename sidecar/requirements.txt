fastapi==0.118.0
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
docker==7.1.0
httpx==0.28.1
apscheduler==3.10.4
# bandcampsync handles all the Fan API auth and pagination — TLS
# impersonation, session cookie handling, paged collection load,
# de-duplication. We use the same library the bandcampsync container
# uses, so identical behaviour. curl_cffi comes in transitively.
bandcampsync==0.7.0
# Playwright (Plan D) — needed because Bandcamp's CDN throttles
# non-browser HTTP clients regardless of TLS+headers+cookies. Real
# browser execution (JS, canvas fingerprint, etc.) bypasses this.
playwright==1.50.0
